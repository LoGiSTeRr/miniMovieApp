<UserControl x:Class="WpfApp1.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WpfApp1.Views"
             xmlns:vc="clr-namespace:WpfApp1.ViewModels"
             d:DataContext="{d:DesignInstance vc:SearchViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="0.5*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox Margin="10 0 10 0" Text="{Binding TextBoxText}"></TextBox>
        <Button Grid.Column="1" Margin="10 0 10 0" Command="{Binding SearchCommand}"></Button>
        <ListBox Grid.Row="1" Grid.ColumnSpan="2" ItemsSource="{Binding SearchResult.results}"></ListBox>
        
    </Grid>
</UserControl>
